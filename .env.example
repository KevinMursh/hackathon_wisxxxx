# 複製這份檔案為 .env 並依實際環境填入；.env 本身已在 .gitignore，不會被 commit。
# 本機開發用 AWS profile 讀取憑證；部署到 EC2 時改用 IAM Instance Profile，
# 不要把 access key 寫進這裡或任何檔案。

# --- Bedrock / Claude Agent SDK ---
CLAUDE_CODE_USE_BEDROCK=1
AWS_REGION=us-east-1
# 本機開發指定 profile；EC2 上留空，改由 Instance Profile 提供暫時憑證
AWS_PROFILE=hackathon

# 已驗證可用的 inference profile（2026-09-12 測試通過，見 scenarios_kc/ 相關紀錄）
ANTHROPIC_MODEL=us.anthropic.claude-sonnet-4-5-20250929-v1:0
ANTHROPIC_SMALL_FAST_MODEL=us.anthropic.claude-sonnet-4-5-20250929-v1:0
# 需要更強法律推理時（如決定書草稿生成）可切換：
# ANTHROPIC_MODEL=us.anthropic.claude-opus-4-5-20251101-v1:0

# --- Bedrock Knowledge Base（法規/判解/函釋/決定書 RAG，若已建立再填）---
# BEDROCK_KNOWLEDGE_BASE_ID=
