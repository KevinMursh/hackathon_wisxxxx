# 訴願智審臺 Prototype v10 確認清單　— 案件進行中補件（簡化版）＋ 助手建議句分「問／改」

> 日期：2026-09-12　情境：卷宗不會一次到齊；補件只做「辨識 → 看結果 → 匯入」，不跳助手、不自動重跑；需要時再到助手要求針對某個爭點重新審查
> 狀態：**全部 ☑（2026-09-12，v10b）**

## 0. 已拍板

| 決定 | 內容 |
|---|---|
| 入口 | 卷宗瀏覽器頂部「＋ 補件」＋左欄拖放 |
| 只跑新檔 | 新檔標「辨識中…」，只有這些送 Claude 判定；既有文件不動 |
| 結果視窗 | 送出後跳「補件辨識結果」視窗：原檔名 → 標準檔名、類型、來源（可直接改）、格式、Claude 摘要／狀態（重複／無法辨識） |
| 匯入 | 按「匯入 N 份」只加入卷宗（標「新」），不重跑任何分析；取消則移除 |
| 需要才改 | 之後在助手說「依新補件重新審查爭點 1」→ 提案卡（主張 diff、新增卷證、AI 將補充之認定）→ 確認 → 局部重跑；爭點分頁的建議句在有新補件時自動出現這句 |
| 建議句分組 | 兩行：`問`（灰框，點了直接送出）／`改`（主色框、✎ 前綴、與提案卡同色；點了填入輸入框讓承辦人補「因為…」再送） |
| 狀態 | 已送審／已結案 → 補件停用 |

## 1. 驗收

| # | 步驟 | 結果 |
|---|---|---|
| Q1 | case02 → 載入示範補件包 | 2 檔「辨識中…」→ 視窗列 2 列：補充理由書（訴願人）、採證照片（訴願人）；既有 18 份未重跑；助手未彈出 ✓ |
| Q2 | 視窗改類型／來源 | 下拉即改；無法辨識列紅底，指定後恢復 ✓ |
| Q3 | 匯入 | 視窗關閉；左欄訴願人分組 4 件、兩檔標「新」；自動開啟第一份；Tab1–5 未變 ✓ |
| Q4 | 再傳同名檔＋scan_0099.pdf | 視窗「辨識 1・重複 1」；重複列灰、無法辨識列紅 ✓ |
| Q5 | 取消 | 待匯入與重複檔自清單移除 ✓ |
| Q6 | 助手 Tab2 建議句 | 「改」行出現「✎ 依新補件重新審查爭點 1」；點了填入輸入框不送出 ✓ |
| Q7 | 送出該句 → 確認 | 提案卡：主張 diff、新增卷證截圖、AI 將補充認定；確認後重跑 3–6，爭點 1 主張與卷證更新、認定附「補件後」；回覆「部分採納・維持原認定」 ✓ |
| Q8 | 「問」句 | 點了直接送出並回答 ✓ |
| Q9 | 已送審 | 補件鈕停用 ✓ |
| Q10 | 400px | 頁面無橫向捲動；視窗表格自身可橫捲；主控台無錯誤 ✓ |

## 3. v11 補充：確認執行後不跳頁（2026-09-12）

| # | 步驟 | 結果 |
|---|---|---|
| S1 | 確認執行 | 畫面留在原分頁，無「處理中」全頁 ✓ |
| S2 | 執行中 | 提案卡六步逐一「…」→「✓」、標題「執行中 2/4：法規推薦」；受影響分頁標籤「● 更新中」、內容淡化不可點 ✓ |
| S3 | 完成 | 只重繪受影響分頁並高亮 1.6 秒；未受影響分頁與卷宗瀏覽器不動；助手接「已執行」卡 ✓ |
| S4 | 執行中切分頁 | 可切到未受影響分頁正常閱讀 ✓ |
