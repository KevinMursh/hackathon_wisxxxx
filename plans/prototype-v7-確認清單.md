# 訴願智審臺 Prototype v7 確認清單　— 首頁極簡化（為評審一眼看懂）

> 日期：2026-09-12　目的：首頁只回答「我要處理哪個案子？」；資料背景與內部說明退位
> 範圍：只改 `prototype/index.html`、`app.js` 的首頁（s-pick）；其餘畫面不動
> 狀態標記：☐ 待做　☑ 完成　✗ 取消

## 0. 已拍板

| 決定 | 內容 |
|---|---|
| 首頁區塊 | 8 → 4：標題 → 上傳（主角）→ 示範案件一列 → 法規庫一行＋關於 |
| 貼上訴願書文字區 | 移除（含黃色說明條）；真運算路徑改由 experiments pipeline 口頭說明 |
| 4 格統計 | 移除；資料規模改寫成標題下一句話 |
| 案件庫狀態列 | 移除；頂部列已有「案件庫」入口 |
| 法規庫狀態列 | 縮成一行：法規 N 部・函釋 N 則・上次同步・N 部資料集版本落後 → |
| demo 載入按鈕 | 降級為上傳區內小字連結（載入 case02 卷宗包／亂檔名／清除） |
| 示範案件卡 | 縮小：案號、標題、一個關鍵標籤、開始分析 |
| 關於 | 留在最底，兩行小字 |

## 1. 清單

1. ☑ 標題縮為一行 h1＋一句說明（含資料規模：101 份決定書、15 部法規、12 則函釋、19 則判解）
2. ☑ 移除 `.stat-row` 4 格統計
3. ☑ 移除 `.paste` 區、`.live-note`；`app.js` 拿掉 pasteBox／parseBtn／sampleBtn 綁定（parseAppeal／buildLive 保留不刪，供 pipeline 參考）
4. ☑ `.intake` 改單欄：上傳區全寬；「開始分析」為唯一主按鈕；demo 載入改為小字連結列
5. ☑ 示範案件改一列三張小卡（案號／標題／一個標籤／開始分析 →）
6. ☑ 移除 `#libCard` 案件庫狀態列與其 render
7. ☑ `#lawCard` 改為一行文字列（可點進法規庫），顯示 N 部落後（由 LAWLIB 的 src 含「落後」算出）
8. ☑ 關於改兩行小字；移除「貼上文字路徑為瀏覽器端規則運算」句
9. ☑ 頂部列、案件庫、法規庫、五分頁、助手不動

## 2. 驗收

| # | 步驟 | 預期 |
|---|---|---|
| K1 | 1440×900 首屏不捲動 | 看到標題、上傳區、開始分析、示範案件列 |
| K2 | 數區塊 | 上傳、示範案件、法規庫一行、關於；無貼文字區、黃條、統計格、案件庫狀態列 |
| K3 | 點「載入 case02 卷宗包」 | 檔案列表出現，開始分析可按，流程同 v6 |
| K4 | 點示範案件 | 三案皆進 s-run |
| K5 | 法規庫一行 | 顯示同步時間與「2 部資料集版本落後」，點了進法規庫 |
| K6 | 400px | 無橫向捲動、主控台無錯誤 |
| K7 | 現有功能 | 案件庫、法規庫、五分頁、助手、結案、法院回填全部正常 |

## 3. 實作紀錄（2026-09-12）

- K1–K7 通過：1440×900 整頁不需捲動；400px 無橫向捲動；主控台無錯誤
- 首頁 `.pick` 寬度 1120 → 960；示範案件卡只留一個關鍵標籤（cardTags[1]）
- 法規庫一行由 LAWLIB 的 src 含「資料集 PDF 為」算出落後部數（目前 2：廢棄物清理法、民法）
- `parseAppeal`／`buildLive`／`SAMPLE_TEXT` 保留在程式中未刪，僅拿掉首頁入口
