# TODO：文書分類清單 27 → 32 類

> 日期：2026-09-13　狀態：☐ 未動工
> 起因：目前 27 類是從「訴願法條文 ＋ 官網表單 ＋ case02 單一卷宗包」拼出來的，
> 用 101 份真實決定書驗證後發現系統性缺漏。**尚未查證官方卷宗目次標準**（見 §4）。

## 1. 證據（101 份決定書全文出現次數）

| 文書 | 次數 | 現況 |
|---|---|---|
| **補正通知函** | 21 | ✗ 缺——77(1)「不合法定程式不補正」10 件的核心文書（§62） |
| **訴願撤回** | 17 | ✗ 缺——77(7) 的另一半（§60） |
| **寄存送達** | 16 | ✗ 缺——決定 30 日起算日，77(2) 核心 |
| **公示送達** | 10 | ✗ 缺——同上 |
| **停止執行申請** | 4 | ✗ 缺（§93） |
| **會勘紀錄** | 3 | ✗ 缺——建築法類常見 |
| 車籍 | 5 | 有，但過度特化（case02 的廢清法煙蒂案專屬） |
| 係數計算表 | **0** | 有，但 101 份裡零出現；一般稱「裁罰基準」（57 次） |

**77(7) 重行提起訴願 10 件**實例：「業經本府訴願審議委員會為訴願不受理之決議在案」
→ 卷內會有**前案決定書**，與結案歸檔的本案決定書用途完全不同，不能共用一個類別。

## 2. 要改的清單

| 動作 | 類別 | nature |
|---|---|---|
| 新增 | 補正通知函 | 紀錄 |
| 新增 | 補正書 | 主張 |
| 新增 | 訴願撤回書 | 主張 |
| 新增 | 送達通知書（寄存／公示） | 紀錄 |
| 新增 | 停止執行申請書 | 主張 |
| 新增 | 會勘查核紀錄 | 紀錄 |
| **拆分** | 委員會決定書 → **前案訴願決定書**（審查中證據，source=本局或第三方）／**本案訴願決定書**（結案歸檔，source=本局） | 紀錄 |
| 一般化 | 車籍資料 → **查詢資料**（車籍／戶籍／登記謄本／金融帳戶交易明細） | 紀錄 |
| 一般化 | 係數計算表 → **裁罰基準計算表** | 紀錄 |

## 3. 動到的檔案

**後端 `server/`**

| 檔案 | 改什麼 |
|---|---|
| `schemas/enums.json` | `doc_type` 27 → 32（單一真相來源） |
| `classify.mjs` | ① `DOC_TYPES` ② `NATURE` 查表 ③ `ORDER` 法定順序表（影響建議檔名序號，新類別要插對位置） |
| `prompts/classify.md` | ① 封閉清單 ② 判定要領補三組辨異：補正通知函 vs 陳述意見通知書、前案決定書 vs 本案決定書、撤回書 ③ source 表補「前案決定書」 |
| `schemas/classify.schema.json` | 從 `classify.mjs` 重新匯出 |
| `eval/manifest.json` | 三年決定書 4 筆正解改「前案訴願決定書」 |

**前端 `prototype/`**

| 檔案 | 改什麼 |
|---|---|
| `data.js` `DOCTAG` | 補 5 個新類別色標（缺色→無樣式） |
| `app.js` `TYPES` | 修正歸戶下拉清單（目前硬寫陣列，要與 enums 同步） |

不動：`normalize.mjs`、`server.mjs`、`store/*`、`api.js`／`toDocs`、部署腳本。

## 4. 未查證事項（重要）

- 上述都是從**決定書（輸出）反推**的間接證據，**沒有查過真正的訴願卷宗目次標準或機關檔案分類表**
- 現場可問法制局承辦人「卷宗目次實際怎麼編」——那才是權威答案
- 或上網找機關公開的訴願卷宗目次範例

## 5. 驗收

- `npm test` 38 案綠（正規化不受影響）
- `node eval/run.mjs --group all` **必須重跑**：新增類別會讓模型重新分配邊界
  （例如原本判「陳述意見通知書」的可能改判「補正通知函」），不重跑不能確認沒退步
- 門檻同前：doc_type ≥90％、source ≥85％、無身分證據→未知 100％、負樣本 100％

估時 30 分鐘，其中一半是重跑驗證。

---

## 附記：mock 資料異常（2026-09-13 發現）

`prototype/data.js` 的 CASE_A：`period = { served: "112-02-07", recv: "114-09-25" }`
→ 逾期 961 日。真實 77② 案件（case01）是逾期 7 日。
時間軸畫出來會極度失衡（合規段只佔 17%）。建議改成貼近真實的值，例如
`served: "114-08-20", recv: "114-09-25"`（逾期 6 日）。此為隊友的 mock 資料，未逕行修改。
